apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "testSpring.fullname" . }}-test-connection"
  labels:
    {{- include "testSpring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "testSpring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
